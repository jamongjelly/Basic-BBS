import React from 'react';
import { AuthModalContainer } from 'src/pages/containers';

const Core = () => {
    return (
        <>
            <AuthModalContainer />
        </>
    );
};

export default Core;
